# Puerto del servidor (default: 4001)
PORT=4001

# Modo producción (descomentar para producción)
# NODE_ENV=production

# Rutas de bases de datos (default: ./restaurante.db y ./restaurante-demo.db)
# DATABASE_PATH=./data/restaurante.db
# DATABASE_DEMO_PATH=./data/restaurante-demo.db
